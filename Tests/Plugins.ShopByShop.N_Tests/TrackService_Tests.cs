using Microsoft.VisualStudio.TestTools.UnitTesting;
using Moq;
using Plugins.ShopByShop.Services;

namespace Plugins.ShopByShop.N_Tests
{
    public class TrackService_Tests
    {
        //[Test]
        //public async Task GetTracksAsync_ShouldReturnEntities_WhenEntityExists()
        //{
        //    _trackService = new TrackService(_sbsConfigMock.Object);

        //    var Tracks = await _trackService.GetTracksAsync();

        //    Assert.That(Tracks, Is.Not.Null);
        //    Assert.That(Tracks.Count, Is.GreaterThan(0));
        //}

        //[Test]
        //public async Task GetTracksAsync_ShouldReturnNull_WhenEntityNoExists()
        //{
        //    _trackService = new TrackService(_sbsConfigMock.Object);

        //    var Tracks = await _trackService.GetTracksAsync();

        //    Assert.That(Tracks, Is.Null);
        //}
    }
}